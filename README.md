# HBM A-share Report

This directory is a sanitized GitHub Pages publish bundle.

- `index.html` is copied from the generated report.
- Do not publish the parent workspace. It contains local data and secrets.
